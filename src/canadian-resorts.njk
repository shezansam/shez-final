{% extends '_templates/base.njk' %}

{% set relative_path = "../" %}

{% block page_title %}Canada{% endblock %}

{% block page_description %}A list of pirate ships of lore from fact and fiction.{% endblock %}

{% block content %}
<article class="container">
  <h1>Top Ski Resorts in Canada</h1>
  <p>Below you will find the top ski resorts in Canada. Located in British Columbia, Alberta, and Québec, these beautiful resorts found in picture perfect mountains will not dissapoint.</p>
  <div class="row row-cols-1 row-cols-lg-3 row-cols-md-2">
  {% for resort in canada.resort %}
  <div class="col my-3">
    <div class="card text-bg-light border-dark">
      <img src="{{ relative_path }}img/{{ resort.image }}" class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title">{{ resort.resort_name }}</h5>
        <h6>{{ resort.location }}</h6>
        <p class="card-text">{{ resort.blurb }}</p>
      </div>
      <div class="card-footer">
        <a href="{{ resort.source }}" class="card-link">More about {{ resort.resort_name }}</a>
      </div>
    </div>
  </div>
  {% endfor %}
  </div>
</article>
{% endblock %}

<!--
  <div class="container">
  <div class="row align-items-center col-md-10 offset-md-2 pb-3">
    <div class="col">

    <div class="row row-cols-1 row-cols-md-2 g-3 mx-2 mb-5">
    {% for resort in overall.resorts %}

    <div class="col">
      <div class="card">
        <img src="{{ relative_path }}img/{{ resort.image }}" class="card-img-top" alt="{{ resort.title }}">
        <div class="card-body">
          <h3 class="card-title"><a href="{{ relative_path }}resorts/{{ resort.slug }}.html">{{ resort.title }}</a></h3>
          <p class="card-text">{{ resort.location }}</p>
        </div>
      </div>
    </div>

    {% endfor %}
 -->